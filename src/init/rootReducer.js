// Core
/*import { combineReducers } from "redux";
import {
  fetchReducer,
  addReducer,
  changeReducer,
} from "../redux/contacts/reducer";
const dataReducer = combineReducers({
  fetch: fetchReducer,
  addUnit: addReducer,
  changeUnit: changeReducer,
});

// Reducers
export const rootReducer = combineReducers({
  converter: dataReducer,
});*/
